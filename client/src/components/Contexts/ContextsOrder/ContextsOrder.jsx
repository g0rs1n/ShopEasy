import { createContext } from "react";

export const CheckIsActivePageContext = createContext('/order/information')
export const OrderDataContext = createContext({})
export const SetOrderDataContext = createContext(null)